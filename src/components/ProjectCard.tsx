import { ArrowUpRight } from 'lucide-react'; import type { Project } from '../lib/types';
export function ProjectCard({project,featured=false}:{project:Project;featured?:boolean}){return <article className={`project-card ${featured?'featured':''}`}><a data-cursor="OPEN" href={project.link ?? '#contact'} target={project.link?.startsWith('http')?'_blank':undefined} rel="noreferrer" className="project-media"><img src={project.image} alt=""/><div className="image-overlay"/><div className="project-meta"><span>{project.category}</span><span>{project.number} / 06</span></div><div className="project-disc">View case <ArrowUpRight size={19}/></div></a><div className="project-info"><div><h3>{project.title}</h3><p>{project.description}</p></div><div className="project-tags">{project.tags.map(t=><span key={t}>{t}</span>)}</div></div></article>}
