import { Container } from '../components/Container'; import { Reveal } from '../components/Reveal'; import { Counter } from '../components/Counter'; import { site } from '../data/site';
export function Metrics(){return <section className="metrics section-dark"><Container><Reveal><div className="section-label light"><span>03</span><i/>By the numbers</div></Reveal><div className="metrics-head"><Reveal><h2>Engineering with <span>evidence.</span></h2></Reveal><Reveal delay={.1}><p>Proof points are part of the interface. We use measurable constraints to keep quality visible from discovery through release.</p></Reveal></div><div className="metric-grid">{site.metrics.map((m,i)=><Reveal key={m.label} delay={i*.08}><article className="metric"><div className="metric-number"><Counter value={m.value} suffix={m.suffix}/></div><div className="metric-label">{m.label}</div><div className="metric-note">{m.note}</div></article></Reveal>)}</div></Container></section>}
