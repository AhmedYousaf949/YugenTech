import { useEffect, useRef, useState } from 'react'; import { ArrowUpRight, Plus } from 'lucide-react'; import { gsap } from 'gsap'; import { Container } from '../components/Container'; import { Reveal } from '../components/Reveal'; import { SectionLabel } from '../components/SectionLabel'; import { site } from '../data/site';

export function Services(){const [active,setActive]=useState(0); const detail=useRef<HTMLDivElement>(null); useEffect(()=>{if(detail.current) gsap.fromTo(detail.current,{x:18,opacity:0},{x:0,opacity:1,duration:.55,ease:'power3.out'});},[active]); return <section id="services" className="services section-light"><Container><SectionLabel index="04">Capabilities</SectionLabel><div className="services-heading"><Reveal><h2>What we <span>engineer.</span></h2></Reveal><Reveal delay={.1}><p>Six disciplines from the Yugen source site, reframed as an interactive operating map.</p></Reveal></div><div className="services-layout"><div className="service-list">{site.services.map(([num,title,desc,tags],i)=><button className={`service-row ${i===active?'active':''}`} key={String(title)} onMouseEnter={()=>setActive(i)} onFocus={()=>setActive(i)}><span className="service-num">{num}</span><span className="service-title">{title}</span><span className="service-arrow"><ArrowUpRight size={20}/></span></button>)}</div><div ref={detail} className="service-detail"><div className="detail-index">{site.services[active][0]} / 06</div><h3>{site.services[active][1]}</h3><p>{site.services[active][2]}</p><div className="tag-grid">{(site.services[active][3] as string[]).map(t=><span key={t}>{t}</span>)}</div><a href="#contact" className="detail-link">Talk about this capability <ArrowUpRight size={17}/></a><div className="detail-graphic"><div className="graphic-grid"/><div className="graphic-orbit"/><div className="graphic-core"><Plus size={28}/></div></div></div></div></Container></section>}
