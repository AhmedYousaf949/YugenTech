import { ArrowUpRight, LockKeyhole, ScanLine, ShieldCheck } from 'lucide-react'; import { Container } from '../components/Container'; import { Reveal } from '../components/Reveal'; import { SectionLabel } from '../components/SectionLabel';
const cards=[['01','Secure by design','Security considerations begin at architecture: auth boundaries, secrets, roles and data flows are part of the first system map.'],['02','Observable by default','Structured logs, useful health checks and deployment signals make the production surface easier to reason about.'],['03','Ready for governance','When a product needs enterprise controls, we design toward least privilege, auditability and secure delivery practices.'],['04','Hardened delivery','CI checks, environment separation and repeatable deployment paths reduce accidental surprises at release time.']];
export function Security(){return <section className="security section-light"><Container><SectionLabel index="10">Security & trust</SectionLabel><div className="security-heading"><Reveal><h2>Built with the <span>guardrails visible.</span></h2></Reveal><Reveal delay={.1}><p>Security is treated as architecture—not a decorative promise at the end of a build.</p></Reveal></div><div className="security-layout"><div className="security-panel"><div className="shield-visual"><ScanLine size={18}/><div className="shield"><ShieldCheck size={45}/></div><div className="scan-beam"/></div><div className="security-status"><span><i/>SYSTEM STATUS</span><strong>SECURE / OBSERVABLE</strong></div></div><div className="security-cards">{cards.map(([n,title,desc],i)=><Reveal key={n} delay={i*.06}><article><span className="security-index">{n}</span><div className="security-icon">{i===0?<LockKeyhole size={17}/>:<ShieldCheck size={17}/>}</div><h3>{title}</h3><p>{desc}</p><a href="#contact">Explore <ArrowUpRight size={15}/></a></article></Reveal>)}</div></div></Container></section>}
